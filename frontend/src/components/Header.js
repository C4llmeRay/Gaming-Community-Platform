import React from 'react';
import Notifications from './Notifications';

const Header = () => {
  return (
    <header>
      <Notifications />
      <p>Header</p>
    </header>
  );
};

export default Header;
